resources:
  requests:
    cpu: "1000m"
    memory: "1Gi"
  limits:
    cpu: "1000m"
    memory: "1Gi"

elasticsearch:
  enabled: false
  external:
    hosts[0]: ELASTICSEARCH-COORDINATING-NODE-NAME
    port: 9200
service:
  type: LoadBalancer
